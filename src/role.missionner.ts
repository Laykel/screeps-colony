// TODO [CARRY, MOVE, CARRY, MOVE, CARRY, MOVE, CARRY, MOVE, CARRY, MOVE, CARRY, MOVE, CARRY, MOVE, CARRY, MOVE]

//const flag = Game.flags['remote-utrium'];
//creep.moveTo(flag);

/*const resource = Game.getObjectById('64b040ae3e758505e9630c4a');
if (creep.pickup(resource) === ERR_NOT_IN_RANGE) {
    creep.moveTo(resource);
}*/

// const storage = Game.getObjectById('64b024ed0848f0458dbbbe66');
// if (creep.transfer(storage, RESOURCE_ENERGY) === ERR_NOT_IN_RANGE) {
//     creep.moveTo(storage);
// }
