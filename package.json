{
  "name": "screeps",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./build/*",
    "build": "pnpm run clean && tsc",
    "watch": "pnpm run clean && tsc --watch"
  },
  "devDependencies": {
    "@types/node": "20.4.0",
    "@types/screeps": "3.3.3",
    "typescript": "5.1.6"
  }
}
